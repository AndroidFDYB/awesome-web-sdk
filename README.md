# 工程目录介绍
`android` 是一个 Android工程
`android/android_web_library` 是一个 Android支持WebView的模块

同理： 

`hm` 是一个 鸿蒙工程
`hm/hm_web_library` 是一个 Android支持WebView的模块

`vue-web` 是一个基于Vite+Vue3的前端工程
`vue-web-sdk` 是一个基于Vite+Vue3的库，可以提供为`vue-web` 使用
